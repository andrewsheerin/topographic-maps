# Current

**Phase:** A — closed (factory migration & as-built baseline)
**Register item:** — (F-1 DONE @ c4dda13)
**Status:** Idle — ready for the next session

## Right now
Nothing in progress. Migration committed on branch `chore/factory-migration`.

## Blocked on
Nothing. Chore C-1 (rotate leaked API key) is the user's to do.

## Next
User to merge `chore/factory-migration` into `main`. Then Phase B — Correctness pass:
resolve D-1 (road-width defaults) and D-2 (carve overlap rule), fix F-3 (shapefile endpoint),
add F-5 (science tests).

---
*Under 20 lines. Detail belongs in the register. Reset at the end of each session.*
